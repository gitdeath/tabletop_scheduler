version: '3'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: tabletop-scheduler
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=file:/app/data/scheduler.db
      - NODE_ENV=production
    restart: always
